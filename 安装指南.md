# 微信推送（wechat-push）· 安装指南

> 这份指南带你把这个 skill 装好、跑通。全程大约 5 分钟。
> 每一步都给了**验证方法**，做完一步确认一步，不要跳步。
> 
> **全程不需要打开命令行，node、python 都由 WorkBuddy 自己提供**——你唯一要提供的，是 skill 文件夹的地址。

---

> ⚠️ **使用前须知**：本项目不是腾讯或 WorkBuddy 官方项目，由第三方独立开发。
>
> 它通过**微信官方的机器人接入服务**（iLink Bot）向你的微信发消息，使用的是
> **你本人扫码获得的凭据**。该接口无公开文档，**可能随时变更并导致本 skill 失效**；
> 请在个人范围内使用，勿用于批量营销、群发、代发等场景。
>
> 完整说明见仓库根目录 **`README.md`** 顶部的《使用前必读》。
> 继续安装即表示你已知悉并接受上述须知。

---

## 先说最重要的一句

**这个 skill 不是「文件夹一放就能用」。**

它更像一台「线路已经接好、但还没插电的电器」——

- **skill 文件**是线路，让 WorkBuddy 装进去就行；
- **「电」是你自己的微信绑定关系**，只能在你自己机器上、用你自己微信扫码建立。

所以下面的 **第 1 步是硬门槛**，谁都替不了你。

---

## 第 1 步：绑定微信 ClawBot（必做）

在 WorkBuddy 里打开**通道 / 连接器设置**，找到与微信相关的 ClawBot（也可能显示为 iLink）通道，
按提示用微信扫码完成绑定。

**怎么确认绑好了？** 看这一点：

- **看得见的方式**：你的微信里出现了一个机器人联系人（名称通常是 `clawbot`）。
  给它发一句话，让它有反应。

---

## 第 2 步：让 WorkBuddy 自动安装 skill

**这一步不需要你手动复制文件夹。** 它是个 skill——你只要把 skill 的**文件地址**告诉 WorkBuddy，安装动作由它自己完成。

在对话框里把 skill 文件夹的路径发过去：

> 安装这个 skill：`<你的 skill 文件夹路径>`

路径要指到**能看到 `SKILL.md` 的那一层文件夹**，例如：

```
C:\Users\<你的用户名>\Downloads\wechat-push
```

WorkBuddy 会把它装进用户级技能目录 `C:\Users\<你的用户名>\.workbuddy\skills\`。
装在这个位置的好处是：**所有项目都能用**，不限于当前这个话题。

**验证方法**：直接问一句「wechat-push 装好了吗」，让它确认；
或直接看下面这个文件是否存在——

```
C:\Users\<你的用户名>\.workbuddy\skills\wechat-push\SKILL.md
```

路径里能看到 `SKILL.md`、`scripts`、`references` 三样东西，就对了。

> 万一自动安装没成功，退一步也可以手动把 `wechat-push` 这个**文件夹**整个复制到
> `C:\Users\<你的用户名>\.workbuddy\skills\`，效果一样。

装好后建议**重启一次 WorkBuddy**，让它重新扫描技能目录。

---

## 第 3 步：测一条消息

**这一步不用开命令行。** 直接在 WorkBuddy 的对话框里发一句话：

> 用微信 ClawBot 推送，给我发一条「看到这条说明通道已打通」

WorkBuddy 会自动调用 `scripts/wb-push.js`，把消息推到你微信。这条路叫**主动推送**。

**验证方法**：微信里收到了这条消息。

- ✅ 收到了 → 通道没问题，往你个人微信发消息的能力已经具备，安装到此完成。
- ❌ 没收到 → 去看文末《常见问题》。
  最常见的原因是**会话不活跃**：打开微信，给 `clawbot` 随便发一条消息（发个「1」就行），
  再让它重发一次。

---

## 第 4 步（可选）：批量删除防护提醒

**以下是可选操作，不是必须的**

WorkBuddy 的「批量删除确认」弹窗不会自动通知到微信，需要额外跑一个守护进程才能推送过去。

先做一次初始化（把已有历史事件标记为「已读」，避免一上来就轰炸你的微信）：

```bash
node "C:/Users/<你的用户名>/.workbuddy/skills/wechat-push/scripts/wb-audit-watch.js" --reset
```

确认无误后，让它常驻运行（需要保持窗口开着）：

```bash
node "C:/Users/<你的用户名>/.workbuddy/skills/wechat-push/scripts/wb-audit-watch.js"
```

> ⚠️ 这两条命令要**在 WorkBuddy 的对话框里让它执行**，不要自己开 cmd 跑——
> 原因见下一节。
> 
> 想要开机自启，可以做一个 `.vbs` 启动器丢进 Windows 启动文件夹，
> 让它以隐藏窗口方式运行。进阶内容，可自行查阅或询问讲师。

---

## 为什么不需要查 node 路径

`wb-push.js` 是个 JavaScript 脚本，需要一个 **node 运行环境**才能跑。这是它唯一的硬依赖。

但你不需要自己装、也不需要自己找——**WorkBuddy 客户端自带一份**：

```
C:\Users\<你的用户名>\.workbuddy\binaries\node\versions\<版本号>\node.exe
```

关键在于：**WorkBuddy 会把这份内置 node 放进自己的运行环境（PATH）里**，
所以你在对话里让它发消息时，它找得到 node，不用你操心。

> ⚠️ **反过来说，这也是一个常见的误判点**：
> 如果你**自己**打开 cmd / PowerShell 去跑 `wb-push.js`，大概率会提示
> `'node' 不是内部或外部命令`。
> 
> 不是装错了——是因为 **WorkBuddy 自带的 node 只在 WorkBuddy 内部可见**，
> 它并没有被写进系统的 PATH。
> 
> 这正是本指南让你「在 WorkBuddy 里测」而不是「去命令行测」的原因。

---

## 常见问题

| 现象                               | 原因与处理                                                                                                    |
| -------------------------------- | -------------------------------------------------------------------------------------------------------- |
| 手动发送时报「未找到微信 ClawBot 凭据」         | 第 1 步没做完。回去绑定微信 ClawBot，确认 `settings.json` 里能搜到 `weixinClawBot`                                          |
| 让 WorkBuddy 发消息，完全没反应也没报错        | 先确认第 1、2 步；再重启一次 WorkBuddy 让它重新加载 skill                                                                  |
| 自己开 cmd 跑脚本，报 `'node' 不是内部或外部命令` | **正常现象**，不是装错了。内置 node 只在 WorkBuddy 内可见——改在 WorkBuddy 的对话框里让它执行即可                                        |
| 报 `ret=-2 prepare failed`        | 推送配额用尽或会话过期。**给微信里的 clawbot 发任意一条消息**，即可刷新                                                               |
| 报 `errcode=-14 session timeout`  | 登录态无效或已过期。先给 clawbot 发条消息激活；仍报错就重新扫码绑定                                                                   |
| 脚本显示「已发送」，微信却没收到                 | 这个通道有**三种静默失败**都可能长这样——① 凭据/登录态无效；② 会话不活跃，消息被静默丢弃；③ 内容超 1500 字被静默截断。**退出码 0 和「已发送」都不等于送达**，唯一可靠的确认是你亲眼看到 |
| 发了几条后突然断了                        | 平台限速：约 7 条 / 5 分钟、单会话约 10 条。等几分钟，或给机器人发条消息刷新会话                                                           |
| 一次性要发很长的内容                       | 超过 1500 字会被**静默截断**。改用分段脚本 `scripts/wb-push-long.py`                                                     |

---

## 一张图记住整条链路

```
你的微信  ←──  iLink 平台  ←──  wb-push.js  ←──  WorkBuddy（对话中）
   ↑                                                      ↑
   └── 前提：你先扫码绑定了 ClawBot          脚本在 WorkBuddy 内运行，
                                            所以 node 不用你操心
```
