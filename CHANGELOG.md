# 版本变更记录

本项目遵循语义化版本。**注意：1.0.1 / 1.0.2 / 1.1.1 为本地开发版本，未单独发布**；
GitHub 仓库首次公开时为 1.0.0，此后各增量累积合并，以 **1.2.0** 的形式重新发布。

因此，若你安装的是 1.0.0，升级到 1.2.0 会一次性获得 1.0.1 以来的**全部**变更。

---

## [1.2.0] — 2026-09-24

> **本次为 GitHub 仓库自 1.0.0 以来的首次更新。**

### 新增：凭据诊断与维护命令

WorkBuddy 5.6+ 将 `settings.json` 中的 `botToken` / `channelId` 改为加密信封
（`{"$wbEncrypted":1,"envelope":"<base64>"}`），密钥由客户端原生层经 IPC 下发，
**外部脚本无法解密**。旧版脚本会把该对象拼成 `Bearer [object Object]`，
导致服务端返回具有误导性的 `errcode=-14 session timeout`。

本次新增 4 个开关，可安全用于排查（**不打印密钥本身**）：

| 命令 | 用途 |
|------|------|
| `--cred-status` | 显示凭据解析路径与实际来源 |
| `--session-status` | 探测会话活跃度，判断消息能否真正投递 |
| `--recover-token` | 从 claw-state / 历史备份回收凭据并写入缓存 |
| `--set-token` | 手工写入凭据缓存 |

### 新增：五级凭据解析链

按序尝试，并对后两级做**账号一致性校验**（重新扫码绑定会更换 `accountId`，旧来源将被拒绝）：

1. 环境变量 `WBPUSH_WX_TOKEN` / `WBPUSH_WX_USER`
2. 本地凭据缓存 `~/.workbuddy/wb-push.credentials.json`（写入权限 `0600`）
3. `settings.json` 中的明文凭据（旧版客户端）
4. **claw-state 轮询游标**（加密客户端下的主用来源）
5. 历史 `settings.json*` 备份回收

### 新增：退出码 3

凭据不可用时，脚本以**退出码 3** 结束并打印补救步骤，不再把 `[object Object]`
当作 token 发出。

| 退出码 | 含义 |
|--------|------|
| `0` | 已受理（**受理 ≠ 送达**） |
| `1` | 网络 / 协议 / 业务失败 |
| `2` | 用法错误 |
| `3` | 凭据不可用 |

### 新增：收不到消息的四步分诊

分离两类此前被混为一谈的成因——**凭据解析失败**（走不通鉴权）与
**会话未建立**（鉴权通过、消息仍被静默丢弃）。后者尤其隐蔽：
换绑后新凭据已下发、接口正常返回 `message_id`，但消息不投递。

### 新增：错误码语义对照表

补充实测对照，纠正此前把错误码按字面理解的倾向：

- `ret=-2` **不专指配额**——参数缺失同样返回该值
- `errcode=-14` 的真实含义是**鉴权层**问题，而非其字面所示的「会话超时」
- `ret=-4` 表示该 bot 下**没有活跃会话**，消息必被丢弃

### 文档

- 明确「**定时任务优先使用客户端原生「推送到微信」开关**」——纯文本场景下，
  平台侧管理凭据，不受客户端凭据加密影响
- README 与安装指南补充 **GitHub 安装流程**与**升级方法**
- 新增本变更记录

---

## [1.1.1] — 未单独发布

### 新增：从 claw-state 游标自动解析凭据

客户端长轮询时会将其身份串（与 `botToken` 同构）写入
`~/.workbuddy/claw-state/weixin/<accountId>_im.bot.cursor.json`。该文件为明文、
由客户端持续刷新、文件名即账号，因此**天然跟随重新绑定**。

脚本自本版起自动读取它（解析顺序第 4 条），加密客户端下开箱可用，无需手工操作。

---

## [1.0.2] — 未单独发布

### 修复：长文本分段的标题重复

正文首行若与消息标题语义重复，会被自动剥离，避免出现
「消息标题 + 正文首行摘要」两行重复。

---

## [1.0.1] — 未单独发布

### 改进：长文本分段改为条目感知

识别 `1. ` / `1、` / `- ` / `• ` 等条目起始行，把每条当作**原子单位**——
放不下就把整条移到下一段，**不再从条目内部夹断**。`##` 小标题会吸附到紧随内容之前。
单条自身超长时按句号边界续拆，续段加前缀 `（接上条）`。

---

## [1.0.0] — 首次公开发布

- 文本推送：`--send`
- 媒体推送：`--send-image` / `--send-file`
- 系统 hook 集成：`Stop`（任务完成）、`PermissionRequest`（高风险操作确认）
- 长文本分段助手 `wb-push-long.py`（规避 1500 字静默截断）
- 审计日志守护进程 `wb-audit-watch.js`（批量删除防护提醒）
- iLink 协议参考与排障手册
