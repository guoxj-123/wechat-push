# 版本变更记录

本项目遵循语义化版本。**1.0.1 / 1.0.2 / 1.1.1 为本地开发版本，未单独发布**；
GitHub 仓库首次公开时为 1.0.0，1.0.1 以来的全部增量合并进 **1.2.0** 发布。

因此，若你安装的是 1.0.0，升级到 1.2.0 会一次性获得 1.0.1 以来的**全部**变更。

---

## [1.2.0] — 2026-09-24

> **本次为 GitHub 仓库自 1.0.0 以来的首次更新。**

### 修复：WorkBuddy 5.6+ 加密凭据导致推送失效

WorkBuddy 5.6+ 将 `settings.json` 中的 `botToken` / `channelId` 改为加密信封
（`{"$wbEncrypted":1,"envelope":"<base64>"}`），密钥由客户端原生层经 IPC 下发，
**外部脚本无法解密**。旧版脚本会把该对象拼成 `Bearer [object Object]`，
导致服务端返回具有误导性的 `errcode=-14 session timeout`，**消息发不出去**。

本版改从客户端轮询游标取材（见下方「五级凭据解析链」），**自动适配，无需手工干预**。

### 新增：凭据诊断与维护命令

新增 4 个开关，可安全用于排查（**不打印完整密钥**）：

| 命令 | 用途 |
|------|------|
| `--cred-status` | 显示凭据解析路径与实际来源 |
| `--session-status` | 探测会话活跃度，判断消息能否真正投递 |
| `--recover-token` | 从 claw-state / 历史备份回收凭据并写入缓存 |
| `--set-token` | 手工写入凭据缓存 |

### 新增：五级凭据解析链

按序尝试，并对后两级做**账号一致性校验**（重新扫码绑定会更换 `accountId`，旧来源将被拒绝）：

1. 环境变量 `WBPUSH_WX_TOKEN` / `WBPUSH_WX_USER`
2. 本地凭据缓存 `~/.workbuddy/wb-push.credentials.json`（写入权限 `0600`）
3. `settings.json` 中的明文凭据（旧版客户端）
4. **claw-state 轮询游标**（加密客户端下的主用来源）
5. 历史 `settings.json*` 备份回收

### 新增：退出码 3

凭据不可用时，脚本以**退出码 3** 结束并打印补救步骤，不再把 `[object Object]`
当作 token 发出。

| 退出码 | 含义 |
|--------|------|
| `0` | 已受理（**受理 ≠ 送达**） |
| `1` | 网络 / 协议 / 业务失败 |
| `2` | 用法错误 |
| `3` | 凭据不可用 |

### 新增：收不到消息的五步分诊

第 ⓪ 步先确认 hook 是否真的执行——入口故障**完全静默**：命令里的路径若写成
反斜杠形式（`C:\Users\…`），bash 会把它当转义符吃掉，脚本根本不会启动，
无输出、无日志、退出码也不可见，后续排查方向会全部跑偏。`--hook-status`
逐个校验已配置事件的命令：路径是否存在、是否含反斜杠、`--hook <事件名>`
是否与所在事件匹配。**未配置的事件只作提示，不算错误**（`Stop` /
`PermissionRequest` 均可按需启用）。hook 模式下的失败会向 `wb-push.debug.log`
追加一条 `error` 记录，供入口静默失效时回溯。

其余四步分离两类此前被混为一谈的成因——**凭据解析失败**（走不通鉴权）与
**会话未建立**（鉴权通过、消息仍被静默丢弃）。后者尤其隐蔽：
换绑后新凭据已下发、接口正常返回 `message_id`，但消息不投递。

### 新增：错误码语义对照表

补充实测对照，纠正此前把错误码按字面理解的倾向：

- `ret=-2` **不专指配额**——参数缺失同样返回该值
- `errcode=-14` 的真实含义是**鉴权层**问题，而非其字面所示的「会话超时」
- `ret=-4` 表示该 bot 下**没有活跃会话**，消息必被丢弃

### 修复：审计守护进程的性能问题

`wb-audit-watch.js` 原为每 2 秒全量读取审计目录下的所有 jsonl 并逐行解析。
该目录随使用天数线性增长（实测 45 天 6.6 MB / 50 文件 / 4869 行），
即持续约 3.4 MB/s 的无效 IO 与 JSON 解析，且随时间单调上升。

改为**文件签名缓存**（`mtime + size`）：往期日账写入后不再变化，整体跳过；
spool 文件签名会变，仍会重扫。实测单轮扫描 **184 ms → 2 ms，降幅 95.9%**。
已消失文件的签名会自动从状态中清理；`--reset` 走强制全量，不受缓存影响。

### 新增：日志上限

- `wb-audit-watch.log` 上限 512 KB，超出后保留最近一半（原为无限增长）
- `wb-push.debug.log` 维持 200 KB 上限，并在文档中明确登记

### 文档

- 明确「**定时任务优先使用客户端原生「推送到微信」开关**」——纯文本场景下，
  平台侧管理凭据，不受客户端凭据加密影响
- README 与安装指南补充 **GitHub 安装流程**与**升级方法**
- 新增**运行时产物清单**（SKILL.md / README）：各文件的用途与上限；均位于
  `~/.workbuddy/` 下、skill 目录之外，可随时删除并由脚本自动重建
- 明确 `wb-push.debug.log` 会记录 `tool_input` 原文，并给出隐私处理建议
- 说明 `context_token` 仅在显式提供时携带（claw-state 回收的凭据不含它），
  当前服务端接受缺省
- 清理代码注释中的版本演化叙述与旁白性表述
- 新增本变更记录

---

## [1.1.1] — 未单独发布

### 新增：从 claw-state 游标自动解析凭据

客户端长轮询时会将其身份串（与 `botToken` 同构）写入
`~/.workbuddy/claw-state/weixin/<accountId>_im.bot.cursor.json`。该文件为明文、
由客户端持续刷新、文件名即账号，因此**天然跟随重新绑定**。

脚本自本版起自动读取它（解析顺序第 4 条），加密客户端下开箱可用，无需手工操作。

---

## [1.0.2] — 未单独发布

### 修复：长文本分段的标题重复

正文首行若与消息标题语义重复，会被自动剥离，避免出现
「消息标题 + 正文首行摘要」两行重复。

---

## [1.0.1] — 未单独发布

### 改进：长文本分段改为条目感知

识别 `1. ` / `1、` / `- ` / `• ` 等条目起始行，把每条当作**原子单位**——
放不下就把整条移到下一段，**不再从条目内部夹断**。`##` 小标题会吸附到紧随内容之前。
单条自身超长时按句号边界续拆，续段加前缀 `（接上条）`。

---

## [1.0.0] — 首次公开发布

- 文本推送：`--send`
- 媒体推送：`--send-image` / `--send-file`
- 系统 hook 集成：`Stop`（任务完成）、`PermissionRequest`（高风险操作确认）
- 长文本分段助手 `wb-push-long.py`（规避 1500 字静默截断）
- 审计日志守护进程 `wb-audit-watch.js`（批量删除防护提醒）
- iLink 协议参考与排障手册
